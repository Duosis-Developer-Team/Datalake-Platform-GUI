apiVersion: v1
kind: ConfigMap
metadata:
  name: bulutistan-customer-api-config
data:
  DB_HOST: "10.134.16.6"
  DB_PORT: "5000"
  DB_NAME: "bulutlake"
  DB_USER: "customer_svc"
  REDIS_HOST: "bulutistan-redis"
  REDIS_PORT: "6379"
  # Matches app/config.py's default; made explicit so the db-per-service
  # split is readable from the manifests alone (frontend 0, customer-api 1,
  # crm-engine 2, datacenter-api 3).
  REDIS_DB: "1"
