apiVersion: v1
kind: Service
metadata:
  name: bulutistan-chatbot-api
  labels:
    app: bulutistan-chatbot-api
spec:
  selector:
    app: bulutistan-chatbot-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
